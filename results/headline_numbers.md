# Headline numbers (exact, model-implied)

All values closed form — no simulation, no seed. Generated by
`scripts/run_exact.py`; full tables in `results/table2_exact.csv`.

## Calibration

| quantity | value |
|---|---|
| `alpha_eqCE` (equal-CE VaR level) | **0.08118** |
| `alpha_thr` (threshold-matched, k_alpha = k_eps) | 0.10666 |
| `alpha_min` (VaR feasibility bound) | 0.01597 |
| exact CE loss of ES (= of equal-CE VaR) | **2.275%** |
| (Y0, k_eps, c) | (0.803420, 0.727553, 1.374471) |
| (eps_min, eps_M) | (0.087629, 0.152614) |

## Equal-CE comparison (welfare cost held identical at 2.275%)

| statistic | ES | VaR equal-CE | contrast |
|---|---|---|---|
| bottom-5% mean $F_T$ | **0.71285** | 0.57360 | **+24.28%** |
| $Q_5$ | 0.78746 | 0.63363 | +0.15383 |
| conditional shortfall | 0.13106 | 0.39524 | **x3.016** |
| $P(F_T<k)$ | 0.24791 | 0.08118 | ES higher |
| $E[(k-F_T)^+]$ | 0.03249 | 0.03208 | |
| CE | 0.985302 | 0.985302 | matched |

* bottom-5% mean improvement: **24.28%**
* conditional-shortfall multiple: **3.016x** deeper under VaR
* ES cuts $E[(k-F_T)^+]$ vs Merton by **45.31%**
  (0.05941 -> 0.03249)

### Nuance to state explicitly in the text

Against the *equal-CE* VaR, ES does **not** win on the unconditional
expected shortfall: 0.03249 vs 0.03208, i.e.
-1.26% — the two are
within 1.3% of each other,
and VaR is marginally lower. The same is true of $P(F_T<k)$, where VaR is
much lower by construction (0.0812 vs 0.2479).

The entire ES advantage is in the SHAPE of the shortfall, not its mean:
ES accepts shortfalls that are frequent and shallow, VaR produces ones that
are rare and deep. That is exactly what the bottom-5% mean
(+24.3%) and the conditional shortfall (3.02x)
measure, and it is why a mean-based summary understates the difference.
Leading with $E[(k-F_T)^+]$ against the equal-CE VaR would invite the
objection that the two constraints are equivalent; leading with the tail
statistics is both stronger and more accurate.

## Nominal alpha = 0.1 (not welfare-matched)

| statistic | ES | VaR |
|---|---|---|
| bottom-5% mean | 0.71285 | 0.59144 |
| conditional shortfall | 0.13106 | 0.36109 |
| CE loss (%) | 2.275 | 1.597 |

VaR's bottom-5% mean (0.59144) is below Merton's
(0.64554): the gambling incentive makes the deep tail
worse than no constraint at all.
